# הנחיות פיתוח קבועות

## 1. שמירת נתונים — עדיפות עליונה
- כל מידע של המשתמש (טקסט + תמונות) חייב להישמר בזמן אמת, ברגע ההזנה
- אחסון כפול: IndexedDB כראשי, localStorage כגיבוי
- כל מוטציה (יצירה, עדכון, מחיקה) חייבת לעשות `await` לשמירה — לא להסתמך רק על useEffect
- להציג אינדיקטור שמירה (toast) כדי שהמשתמש יראה אישור: "שומר" / "נשמר" / "שגיאה"
- להציג מסך טעינה בעת הפעלת האפליקציה בזמן שהנתונים נטענים, למניעת הבזק של מצב ריק
- לעולם לא לבלוע שגיאות אחסון בשקט — תמיד להציג אותן למשתמש

## 2. טיפול בתמונות
- לדחוס כל תמונה לפני אחסון: מקסימום 1200x1200 פיקסלים, איכות JPEG 0.6
- זה מקטין תמונות מ-5-10MB ל-50-80KB בערך
- לדחוס בהעלאה, לפני השמירה — לעולם לא לאחסן תמונות בגודל מלא

## 3. סנכרון בין מכשירים
- האפליקציה חייבת לעבוד גם במחשב וגם בנייד — אותו מאגר מידע
- סנכרון ענן דרך GitHub API — קריאה/כתיבה לקובץ data בענף `data`
- שמירה מקומית (IndexedDB) + דחיפה אוטומטית ל-GitHub בכל שמירה
- בטעינה: מיזוג נתונים מקומיים עם נתונים מרוחקים (לפי project ID)
- כפתור סנכרון ידני + הגדרות Token

## 4. בטיחות פריסה (Deploy)
- פריסת קוד חדש לעולם לא תמחק נתוני משתמש קיימים
- הנתונים חיים בדפדפן (IndexedDB/localStorage) + GitHub ענן — לשמור על הפרדה
- תמיד לבנות (`npx vite build`), ואז לפרוס את dist ל-gh-pages

## 5. רספונסיביות — חובה
- כל מסך חייב לעבוד טוב גם במחשב וגם בנייד
- breakpoints: 768px (טאבלט), 600px (נייד), 380px (נייד קטן)
- גריד ספריה: עמודות מתכווצות ל-1 בנייד
- כפתורים, טקסט, ושדות מתאימים את הגודל למסך
- header עם flex-wrap כדי לא לגלוש

## 6. סטנדרטי עיצוב
- תצוגה עברית RTL (dir="rtl", lang="he")
- 3D glassmorphism: עומק, אפקטי ריחוף, צללים, כדורי רקע מונפשים
- מתג מצב בהיר/כהה עם CSS variables
- תחושה פרימיום: שדות neumorphic, כרטיסי זכוכית, זוהר עדין
- בכל שיחה לאפשר מקורות השראה לעיצוב ושינויים כך שהאפליקציות משודרגות ומגוונות כל הזמן
- מינימום אינטראקציות על דברים חשובים לתפעול — לשדרג בלי לשבור

## 7. חוויית משתמש מלאה
- תמיד לתת גם מצב תצוגה וגם מצב עריכה — לאפשר תיקון מתוך האפליקציה
- עריכה של טקסט, תמונות, והוספת/הסרת פריטים — הכל מתוך הממשק
- לא לכפות על המשתמש למחוק וליצור מחדש כדי לתקן טעות

## 8. כפתור קישור לעריכה (Claude Code)
- בכל אפליקציה חדשה — להוסיף כפתור עריכה (✎) בכותרת כדבר ראשון
- הכפתור פותח את סשן Claude Code הנוכחי בטאב חדש לשדרוג האפליקציה
- עיצוב: כפתור עגול סגול עם אייקון עט, תואם לשאר כפתורי הכותרת
- הקישור: `https://claude.ai/code/session_<SESSION_ID>` — להחליף בכל סשן חדש

## 9. טבלת סיכום רשומות
- בכל אפליקציה — להוסיף דף טבלה עם כל הרשומות והעמודות הרלוונטיות של האפליקציה
- כל העמודות ניתנות למיון (עולה/יורד) בלחיצה על כותרת העמודה
- הרשומות מקובצות לפי הישות הראשית (פרויקט/קטגוריה וכד') עם אפשרות כיווץ/הרחבה
- קישורים ישירים מכל תא לתצוגה הרלוונטית באפליקציה
- כפתור ניווט לטבלה בכותרת הראשית

## 10. תהליך עבודה
- תמיד לעשות commit ו-push לענף המיועד
- לפרוס ל-gh-pages אחרי כל שינוי משמעותי
- לנקות קבצי assets ישנים אחרי כל פריסה
- לבדוק את האתר החי אחרי פריסה
